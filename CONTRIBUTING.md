# 参与贡献

感谢你帮助改进这份 GitHub 零基础指南和 Novel Forge 小说写作工程系统。

## 可以贡献什么

- 修正术语、错别字或过时描述
- 补充适合零基础用户的例子
- 报告链接、命令或页面说明的问题
- 改进教程结构和可读性
- 补充不同操作系统下的注意事项
- 改进小说项目模板、提示词、状态账本和确定性质量检查

## 提交前检查

1. 确认修改没有把个人信息、Token 或密钥放进仓库。
2. 如果新增命令，请在对应系统中验证命令用途。
3. 如果修改许可证说明，请保留谨慎、非法律意见的表述。
4. 如果修改 GitHub 页面说明，请尽量避免把可能变化的界面写成永久规则。
5. 让一个没有编程基础的人读一遍，确认步骤不会跳步。
6. 如果修改小说系统，确认 AI 候选稿没有被写成自动正史，且没有引入
   本地模型、外部 API 或不可复现的隐式依赖。
7. 运行 `python -m unittest discover -s tests -v`、状态校验和相关文本审计。

## Issue 建议

标题尽量直接，例如：

```text
Windows 下载说明缺少 exe 校验提醒
```

正文请包含：

- 你看到的原文
- 你认为的问题
- 建议的修改方式
- 如果是操作问题，请附系统和复现步骤

## Pull Request 建议

一个 Pull Request 尽量只处理一类问题。描述中说明：

- 为什么要改
- 改了什么
- 如何检查
- 是否需要同步修改 README 或网页

小说系统相关的 Pull Request 还应说明：

- 修改影响了哪个状态文件、模板或质量门
- 是否改变了读者承诺、角色知识边界或钩子/回报账本规则
- 是否新增了可测试的样例和失败案例
- 是否仍然保留 `rank_scout_status=data_required` 的证据边界
